import styled from "@emotion/styled"

const LogoCustom = styled.div`
  font-weight: 700;
  font-size: 2rem;
  text-transform: capitalize;

`
const Logo = () => {
  return (
    <LogoCustom>
      shop
    </LogoCustom>
  )
}

export default Logo